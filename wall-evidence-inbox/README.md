# 云南墙体临时证据收件箱

此目录由云南墙体与墙面工作室自动写入，仅保存用于知识整流的去 EXIF JPEG 预览与批次 manifest。

- 原始照片与 RAW 默认保留在用户浏览器本地，不上传。
- 每批资料位于 `wall-evidence-inbox/batches/<batch-id>/`。
- `wall-evidence-inbox/latest.json` 指向最近一批资料。
- 完成知识整流并经用户确认后，可删除整个 `wall-evidence-inbox` 临时分支。
