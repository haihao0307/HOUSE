# Codex 接续指令

完整阅读本包的 `START_HERE.md`、`ARCHITECTURE_MASTER_HANDOFF.md`、`CURRENT_STATE.md`、`NO_PHOTO_TASK_SCOPE.md`、统一方法文件、蓝图知识文件和当前审计证据。

## 仓库与保护

仓库：`haihao0307/HOUSE`

开发分支：`feature/yunnan-component-studio-v1`

打包基线：`4a92c2ceb5a6bc9d1289fe3149c0d332a17915b0`

先重新确认远端分支HEAD。若已有后继提交，正常快进后工作。保持PR #13 open、Draft、未合并。禁止强推、改写历史、修改main、gh-pages、Pages设置、其他Mother、冻结资产和权威真值。

本包分支只用于下载与交接，不作为大范围合并源。

## 专业目标

制作一栋能够从底部向上解释和生成的完整云南传统建筑。先选定证据最可靠的基准案例。内部可以逐阶段实现，正式展示需要完整三维建筑，并提供建造阶段、平面、立面、剖面、结构和关键节点的直观切换。

不得恢复旧整栋候选作为新母版。旧生成器只用于失败回归和对照。

## 固定输出边界

不执行照片生成、照片修复、宣传图或单纯静态出图。参考影像只作证据。主要成果是Three.js三维建筑、网页工作台、结构化知识、测试和版本回执。

## 下一轮实现顺序

1. 审查当前可用案例，选择完整度最高的基准建筑，记录选择理由和缺口。
2. 建立统一轴网、标高和部位ID。
3. 生成场地、地基、墙脚和柱础。
4. 生成两榀相邻构架和完整进深，核对支承、接合与安装顺序。
5. 加入楼面、楼梯、墙体、门窗、屋架、基层和瓦作。
6. 扩成一座房屋，再组成完整院落。
7. 建立从地基到完工的阶段控制，所有阶段共享同一对象身份。
8. 建立平面、正立面、侧立面、控制剖面、结构透视、完整三维视图。
9. 保持中性、工作室和诊断模式，同一源状态切换。
10. 增加主动查错：支承连续、轴线同步、墙顶闭合、屋面搭接、排水、门窗、楼梯、遮挡和大角度旋转。
11. 桌面和移动端分别进行真实浏览器测试。
12. 提交精确版本、公开入口、测试、证据、未完成项和批准状态。

## 完成线

向用户报告前，先自行修复明显穿插、悬空、错位、比例失调、无法建造和排水中断。用户负责最终视觉采用，日常建筑判断与质量责任由执行者承担。

`visualApproved=false`

`productionApproved=false`
