# HOUSE / Brick Mother execution rules

## 用户验收输出硬规则

Brick Mother 以及从本分支继续的任何执行者，默认禁止调用图像生成或图像编辑工具。禁止生成概念图、效果图、预览图、参考图、海报、缩略图，也禁止用静态图片替代真实三维成果。

只有用户在当前对话中明确要求“生成图片”“做图片”“修改图片”“给截图”等图片输出时，才允许进入图片工具流程。

用户提出“做一个看看”“试试做一个”“给我看成果”“参考这个形状做一个”时，默认含义是继续修改真实三维系统：程序化几何、三维文件、GLB（如用户要求）、可交互 HTML、Three.js/WebGPU 工作台或对应生产源码，而不是图片。

用户提供 GLB/3D 参考时，必须优先读取其几何、节点、比例、轮廓、边缘和表面形状关系，并把这些信息蒸馏到程序化三维重构中；不得擅自把 GLB 参考转换成生成图片任务。

浏览器自动化可以内部保存 QA 截图，仅作为运行与视觉检查证据。未经用户明确要求，不把 QA 截图作为创作成果发送。

## Brick Mother 当前边界

已认可的 B 材质保持冻结。后续形状、边缘、残缺和磨蚀调整只改几何，不用新噪声重做已认可材质。旧固定版本保留，新成果使用独立三维工作台和固定公网 HTTPS 地址验收。
