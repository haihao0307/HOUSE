# R7 direct delivery marker

This commit is intended to freeze the browser-verified, uncompressed, self-contained `Brick_Mother_R7_Direct.html` for a fixed raw.githack link.

The direct file contains no launcher fetch, no runtime base64 decoding and no runtime gzip decompression. Visual approval remains pending.
