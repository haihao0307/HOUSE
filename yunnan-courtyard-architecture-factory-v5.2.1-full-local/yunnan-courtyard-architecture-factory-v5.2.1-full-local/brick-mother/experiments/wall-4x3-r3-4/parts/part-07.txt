function resetCamera(view='iso'){setViewButtons(view);if(scene==='mother'){const a=Math.max(.46,.46/Math.max(.45,stage.clientWidth/Math.max(1,stage.clientHeight)));const p={iso:[-.62,.31,a,[0,.027,0]],front:[Math.PI,0,a,[0,.030575,0]],back:[0,0,a,[0,.030575,0]],end:[-Math.PI/2,0,a,[0,.030575,0]],top:[Math.PI,Math.PI/2,a,[0,.030575,0]],plaster:[-.62,.31,a,[0,.027,0]]}[view];[yaw,pitch,distance,target]=p;}else if(view==='plaster'){[yaw,pitch,distance,target]=[3.02,.12,2.05,[.68,1.58,-.248]];}else if(view==='core'){[yaw,pitch,distance,target]=[-2.35,.08,1.75,[-1.55,1.52,-.205]];}else{const fit=fitDistance(view==='iso'?1.18:1.05),p={iso:[-2.48,.26,fit,[0,1.5,0]],front:[Math.PI,0,fit,[0,1.5,0]],back:[0,0,fit,[0,1.5,0]],end:[-Math.PI/2,0,fit,[0,1.5,0]],top:[Math.PI,Math.PI/2,fit,[0,1.5,0]]}[view];[yaw,pitch,distance,target]=p;}schedule();}
function updateUI(){const count=scene==='mother'?1:scene==='soil'?0:activeBrickCount(activeCourse),h=activeBrickHeight(activeCourse);document.getElementById('modeTitle').textContent=scene==='wall'?`整墙 · 第 1—${activeCourse} 皮`:scene==='bricks'?`仅砖 · 第 1—${activeCourse} 皮`:scene==='soil'?'墙心显微形体 · 独立检查':'R2.14.8.1 固定母砖';document.getElementById('modeMeta').textContent=scene==='mother'?'母砖形体与材质基准不改':scene==='wall'?`${count} 块砖（基础 ${baseBrickCount(activeCourse)} / 端部闭合 ${closureBrickCount(activeCourse)} / 顶部封砖 ${capBrickCount(activeCourse)}）· 土芯顶 ${Math.max(0,h-.01).toFixed(3)} m${showPlaster?' · 抹灰试片可见':''}`:scene==='soil'?`土芯顶 ${Math.max(0,h-.01).toFixed(3)} m · 前后、顶部与两端均有真实 SDF 起伏 · 切换“墙心近看”观察`:`${count} 块砖 · 每砖差异为确定性，不随刷新跳变`;document.getElementById('courseBuild').disabled=scene==='mother';for(const id of ['soilShape','soilScale','soilLayers','soilColor'])document.getElementById(id).disabled=!['wall','soil'].includes(scene);document.getElementById('plasterShape').disabled=scene!=='wall';document.getElementById('brickVariation').disabled=['mother','soil'].includes(scene);document.getElementById('plasterToggle').disabled=scene!=='wall';schedule();}
document.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{scene=b.dataset.mode;document.querySelectorAll('[data-mode]').forEach(x=>{const on=x===b;x.classList.toggle('active',on);x.setAttribute('aria-pressed',String(on));});resetCamera('iso');updateUI();});document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>resetCamera(b.dataset.view));document.getElementById('plasterToggle').onclick=()=>{showPlaster=!showPlaster;const b=document.getElementById('plasterToggle');b.classList.toggle('active',showPlaster);b.setAttribute('aria-pressed',String(showPlaster));b.textContent=`抹灰试片：${showPlaster?'开':'关'}`;updateUI();};
const bind=(id,fn)=>document.getElementById(id).addEventListener('input',e=>{fn(Number(e.target.value));updateUI();});bind('courseBuild',v=>{activeCourse=v;document.getElementById('courseValue').value=`${v} / 43`;});bind('soilShape',v=>{soilShape=v*.001;document.getElementById('soilShapeValue').value=`±${v.toFixed(1)} mm`;});bind('soilScale',v=>{soilScale=v;document.getElementById('soilScaleValue').value=`${v.toFixed(2)}×`;});bind('soilLayers',v=>{soilLayers=v;document.getElementById('soilLayersValue').value=String(v);});bind('soilColor',v=>{soilColor=v;document.getElementById('soilColorValue').value=`${Math.round(v*100)}%`;});bind('brickVariation',v=>{brickVariation=v;document.getElementById('brickVariationValue').value=`${Math.round(v*100)}%`;});bind('plasterShape',v=>{plasterShape=v*.001;document.getElementById('plasterShapeValue').value=`±${v.toFixed(1)} mm`;});
canvas.addEventListener('pointerdown',e=>{drag=true;last=[e.clientX,e.clientY];canvas.setPointerCapture(e.pointerId);});canvas.addEventListener('pointermove',e=>{if(!drag)return;const dx=e.clientX-last[0],dy=e.clientY-last[1];last=[e.clientX,e.clientY];yaw-=dx*.006;pitch=Math.max(-1.5,Math.min(1.5,pitch+dy*.006));setViewButtons('');schedule();});for(const ev of ['pointerup','pointercancel','lostpointercapture'])canvas.addEventListener(ev,()=>drag=false);canvas.addEventListener('wheel',e=>{e.preventDefault();distance=Math.max(scene==='mother'?.24:.65,Math.min(scene==='mother'?1.4:30,distance*Math.exp(e.deltaY*.001)));schedule();},{passive:false});canvas.addEventListener('webglcontextlost',e=>{e.preventDefault();lost=true;fail('图形上下文已中断，请刷新页面。');});new ResizeObserver(()=>{if(currentView)resetCamera(currentView);else schedule();}).observe(stage);window.addEventListener('resize',schedule);resetCamera('iso');updateUI();
}catch(error){console.error(error);fail('R3.4 着色器初始化失败：'+error.message);}}
</script>
</body>
</html>
