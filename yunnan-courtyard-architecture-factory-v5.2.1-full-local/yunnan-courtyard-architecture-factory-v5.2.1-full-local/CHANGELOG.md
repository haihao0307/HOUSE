# 变更记录

## PR #13 完整墙体系统 V3.0 可视验收

1. 修复拆分运行时初始化阶段错误调用 `tranverse` 导致页面无法生成墙体的问题，并将墙体生产线默认入口切换到 `wall-system-lab.html`。
2. 石基顶层与首层土坯改用实际世界包围盒横向采样，墙顶闭合、石土最大空缝、墙心开边和抹灰面积均来自运行几何，不再使用固定结果充当验收。
3. 外灰与内灰保留独立几何和覆盖参数；覆盖为零时不生成片层，非零覆盖按可用墙面面积分配并限制在墙顶、石基、收分边界和门洞之外。
4. 圆蚀半径与倒角直接受参数控制，土坯和石块使用同一世界坐标连续侵蚀场；石块宽度离散真实受 `stoneIrregularity` 控制，随机种子输出可复现指纹。
5. 浏览器 QA 改为外墙、正面、内墙、抹灰断口、石土节点、圆蚀近景、重蚀、裸土和双种子多视角证据，并在失败时仍保存诊断截图与精确 head 报告。

## PR #13 组件工作台 V2.6.1 数据库迁移修复

1. 将 `YunnanComponentStudio` 与 `YunnanWallStudioV2` 统一交给单一 Schema V2 管理器，移除各墙面脚本分别维护版本和建表逻辑的分叉。
2. 历史空 V1 数据库会通过原子升级补建 `attachments`、`moduleId` 索引和 `previews`，保留已有 store、记录、Blob、哈希和缓存，禁止自动删除数据库。
3. 所有连接加入 `versionchange` 自动关闭与 `blocked` 可见提示，避免 iPhone 后台旧标签页长期阻断升级。
4. 墙面浏览器 QA 同时覆盖缺少目标 store 的空 V1 库、保有真实附件/预览 Blob 但缺少索引的 V1 库，以及旧标签页阻塞升级的场景；验证升级至 V2、Blob 字节与哈希保留、刷新持久化、资料窗口解除卡死，并继续执行 36 张资料、ZIP 与 GitHub mock 全链路。

## 5.5.0

1. 将数据驱动的墙面、瓦顶风化系统接入完整一颗印正式生成器，三个预设的参数实际进入材质 uniform、实例综合色和连续片区生成。
2. 七个屋面单元在生成时绑定固定 ID，每个单元具有非空真实几何、独立标高、坡向、排水目标和构造树；左右耳房使用镜像长短坡，小厦由三个真实披檐段组成。
3. 板瓦与筒瓦改为独立闭合曲面和共享实例批次。V5.5.0 筒瓦列严格等于板瓦列减一并压相邻纵缝，移除旧版顺坡偏移；滴水、勾头、檐板、正脊与封头进入可检查语义层。
4. 屋面增加檩条、椽列、基层、板瓦、筒瓦、檐端和脊部七层真实几何，支持基于原始位置的爆炸与复位。
5. 稳定低频场生成灰尘、日晒、雨蚀、苔藓、磨损、成片缺瓦、崩损和有边界修补。墙面增加抹灰、裸土、草纤维、石勒脚、砖包角、返潮、雨痕、剥落、裂缝、污渍及修补实体层。
6. A/B 页面改为完全本地 Three.js 运行，V5.4.4 和 V5.5.0 使用相同建筑种子、相机、背景与灯光，提供预设、分层、近景和自动演示。
7. 门窗改为真实枢轴装配；人物路线穿过南门并使用一座含上下落脚面、中间平台与连续扶手的 8+8 双跑楼梯到达二层。
8. 新增严格 Playwright 冒烟、完整生产线回归、桌面与移动端 Pages 校验、部署 SHA 校验、性能统计，以及覆盖 11 类检查的 16 张 Surface QA 截图制品。Playwright 或浏览器缺失时测试明确失败。
9. 旋转 90 度的耳房与小厦屋面统一采用 `Qy × Qx` 坡向组合，板瓦、筒瓦、椽条、基层、滴水、勾头和垂脊均由实例矩阵、世界包围盒及排水路径进行几何验收。
10. 增加独立勾头端部、按屋面类型生成的垂脊与封口、每坡局部破损统计，以及七个屋面各自非空的七层构造计数；瓦壳在保留闭合弧面和全部实例的前提下降低细分数。
11. 墙面片层按收分墙体的局部表面生成，山墙排除悬空勒脚与返潮；日晒、檐口遮蔽、排水和重力对材质或几何结果的影响由场景测量验证。
12. 门窗验收加入宿主、唯一构件 ID、实际开合角、铰轴漂移、净宽和碰撞包络；楼梯补齐梯梁、承托、栏杆柱与平台连接扶手，人物路线逐踏步播放并逐帧执行射线支撑和碰撞审计。
13. A/B 显示两侧统一由当前生成器创建同一完整结构壳体，左侧实际执行提交 `323a893a791b1d064a1591dcbd2063f2f6a172c1` 冻结的 V5.4.4 材质与风化工厂，右侧执行 V5.5.0 工厂；QA 强制实际结构指纹相等，并从真实编译 shader 读取运行时分支证据。冻结的完整 V5.4.4 生成器继续作为独立可执行来源证据。
14. 人物浏览器回归以 300 个真实落脚/支撑样本和 335 个相邻落脚段胶囊扫掠点执行最大 0.079874 m 的路线审计；测试请求至少 36 个渲染完成帧，硬门槛为至少 30 帧和 25 个唯一世界位置，本轮一次隔离本地验收实际为 57/57。二层到达、墙体/开口/栏杆碰撞、支撑、悬空和停滞均为硬失败条件。
15. 椽条与檩条改为逐坡实例批次，板瓦、筒瓦及脊部收边复用几何并按坡面合批；墙体结构与八类表面语义合并为 9 个保留 RGB 顶点色和独立 alpha 的静态索引批次，避免透明度在内置顶点色与自定义 shader 中被重复相乘；裂缝和草纤维分别收敛为单一实例批次与静态顶点批次。完整 V5.5.0 场景由 715884 renderer 三角面、715280 场景三角面、12493 实例、1526 draw calls、1316 mesh 优化到 574612、574820、13061、407、407；本轮一次单次无重试 SwiftShader 样本为桌面 7.479 FPS、移动 8.226 FPS。
16. GitHub 同步按运行时注入的 Draft head SHA/ref 读取四个必需数据文件；只有 GitHub REST 未认证限流的 403/429 被建模为可选诊断，其他 HTTP、控制台、页面与请求失败均进入进程失败计数。
17. 完整生产线回归分别加载团结乡、大理和乌龙村模型，保存三张独立 WebGL 画面并校验像素指纹；团结乡高精度主档只允许本地文件选择，不再产生公开缺失资产请求。
18. Surface QA 使用实际几何包围盒生成檐口、屋脊、门窗、人物路线和 8+8 双跑楼梯相机，按 run-scoped 目录生成 JSON 与截图；Draft Pages 部署与公开验证任一步失败或跳过都会使工作流失败。
19. 修正板瓦横截面弧高和坡面法线落位，验收直接投影实际实例顶点并要求瓦壳高于基层；檐口、七层爆炸、脊部、靠墙收口、门窗与楼梯截图采用独立可恢复的 QA 取景状态。
20. 墙面草纤维、裂缝、砖包角及风化覆盖层改为静态合批或实例批次，透明薄片使用单次渲染；渲染 Mesh 的静态局部矩阵被冻结，门窗铰轴、人物和屋面爆炸父组保持活动；最终 draw calls、mesh、三角面和单次无重试 SwiftShader FPS 统一采用第 15 项所列的当前验收口径。
21. 390×844 生产线布局移除 720 px 纸面最小宽度并逐项校验文档、Canvas、控制栏和关键容器边界；移动端 QA 使用经过 PNG 尺寸校验的固定视口截图。
22. Surface workflow 纳入发布包构建、完整 diff 空白检查、Surface QA、完整生产线回归及统一最终 gate；回归截图逐张记录真实相机、viewport、结构指纹、可用表面指纹和证据限制。
23. 屋面与浏览器 QA 同时锁定七屋面 `2+2+2+2+1+3+2=14` 个坡面；16 张 Surface PNG 逐张解码并校验非空像素、颜色变化、SHA-256 和 run-scoped 元数据。
24. Pages 公开 QA 先轮询 `build.json`、Surface Lab 与风化 seed 三项资源至同一 SHA/ref 就绪，再执行完整浏览器交互与零错误/零 4xx/5xx 验收。

# 更新记录

## 5.4.2

1. 参考模型查看器新增“云南大理参考”和“浩思一·乌龙 WL”两个 GLB 入口，与团结乡高精度/网页标准档共用同一套切换、复位、分组和下载逻辑。
2. 两个超过 GitHub 单文件限制的原始 GLB 保留为本地选取源；公开目录只发布不改变几何拓扑的贴图降采样网页档，并明确记录源文件与公开文件哈希。
3. 新增 `data/evidence/yunnan_reference_glb_catalog_v5_4_2.json` 与 `data/qa/yunnan_reference_glb_validation_v5_4_2.json`，把几何、贴图、来源边界和网页加载验收纳入生产线数据合同。
4. GitHub 同步桥、404 入口、版本元数据与浏览器冒烟测试统一升级到 V5.4.2。

## 5.4.1

1. 公开生产线新增 GitHub 同步中枢：读取仓库中的母系统、Three.js 生产合同、团结乡材料证据和最新提交。
2. 网页命令、现场观察、纠错复核和新增证据可进入本地可审计队列，并导出 `yunnan-production-web-sync-*.json`。
3. 新增可选 GitHub Issues 回传：Fine-grained token 只在当前页面内存中使用，不写入源码、localStorage 或导出文件；回传记录统一使用 `[Web Sync]` 标题，便于生产线和后续资料整理同步。
4. 404 入口与主页面统一为 V5.4.1，并保留无令牌只读访问。

## 5.4.0

1. 新增 `threejs/YunnanMaterialFactory.js`，把团结乡样本确认的风化土墙、旧木、云南板瓦—筒瓦和石基外观拆成可复用的 Three.js PBR/程序化风化材质层。
2. 新增 `threejs/YunnanCourtyardProduction.js` 与独立预览页，按滇中一颗印的平面和构架语法生成收分土墙、圆柱、门洞木作、小窗、柴屋、日常楼梯、石板地面及独立逐片瓦顶。
3. 新增生产体系合同、证据边界和 QA 记录；团结乡瓦件精确尺寸、搭接、基层和墙体成因仍保持未锁定，不把扫描 GLB 当作 BIM 构件源。
4. 生产线网页新增 Three.js 参数化生产原型入口，支持按 walls、roof-tiles、timber-frame、doors-windows、stone-and-ground 分组检查。

## 5.3.6

1. 将团结乡样本 01 高精度扫描中的长期风化土墙、典型云南板瓦—筒瓦视觉关系和旧木构综合色登记为用户确认的实物参考。
2. 新增独立材质与风化证据合同，分别记录用户确认、直接观察、专业解释、未锁定项和扫描瑕疵排除项，避免把风化误判为贴图错误或把摄影测量破边当成构造。
3. 新增三套可复用生产规则：分区式土墙风化、离散板瓦—筒瓦旧瓦面、低饱和灰褐至深褐旧木色；同时禁止连续波纹板替代、均匀噪声墙面和高饱和亮漆木材。
4. 网页“团结乡样本”页同步公开已确认的材料证据与仍待测的瓦片尺寸、搭接、基层、墙体配比、木种和涂层。

## 5.3.5

1. 新增 `YN_TUANJIE_001_EDITABLE_HIGH.glb` 高精度可编辑主档：保留原始 High 扫描的 48 个网格、608,634 个顶点、464,288 个三角面，以及 7000×7000 底色与 2500×2500 法线贴图。
2. 原 `YN_TUANJIE_001_EDITABLE.glb` 明确改称网页标准档；它和高精度档几何完全一致，差异主要是 3072×3072/1024×1024 的贴图降采样及文件体积。
3. 团结乡查看器新增高精度/网页标准档切换，实际应用法线贴图、各向异性纹理过滤，并将高 DPI 渲染上限由 1.5 提高至 2.5。
4. 页面与 QA 公开当前加载档位、纹理分辨率和设备纹理上限；扫描源已有的漏空、碎边和摄影测量噪声继续保留为证据边界，不用贴图放大伪装成几何修复。

## 5.3.4

1. 修复直接双击 `index.html` 时，浏览器因 `file://` 权限禁止脚本自动读取相邻 GLB、导致团结乡查看器为空的问题。
2. 查看器不再静默隐藏；新增本地 GLB 文件选择和拖放入口，下载后的 `YN_TUANJIE_001_EDITABLE.glb` 可重新载入网页。
3. HTTP/启动脚本模式仍保留一键打开内置 GLB，并为读取失败、文件类型错误和本地权限限制提供可见提示。
4. 浏览器回归测试同时覆盖 HTTP 内置主档和 `file://` 本地文件两条路径。

## 5.3.3

1. 新增 `YN_TUANJIE_001_EDITABLE.glb`：保留 High 扫描全部 48 个网格与 464,288 个三角面，使用稳定名称和五个可编辑空间分组，内嵌经压缩的底色与法线贴图。
2. 生产线网页新增“团结乡样本”页签；用户点击后才载入约 45.5 MB GLB，可旋转、缩放、复位并按场地、未定细部、一层、二层和屋顶分组开关。
3. FBX、源贴图与旧 High/Lite GLB 继续作为私有证据链保存，但不再是网页运行依赖；新主档无需 Draco 或其他外部解码器。
4. 新增可编辑 GLB 资产合同、派生证据记录和 QA，分开列出当前可进入生产线的观察与必须等待实测或近照锁定的内容。
5. 纠正 5.3.2 对“动画、骨骼和 morph target 转换器冗余”的误判：直接 GLB 审计结果均为 0，48 个条目是扫描空间网格而不是 morph target。

## 5.3.2

1. 将用户确认来自云南团结乡的完整院落扫描登记为稳定样本 `YN_TUANJIE_001`，保留 `YN_C2_Meirenkao` / “民居 C2 美人靠”作为旧路径兼容别名。
2. 新增团结乡连续样本目录、样本 01 完整建筑拆分档案和登记说明，分开记录用户确认、扫描可见、地域推断、未知项、可复用目标与禁止提取项。
3. 修正原尺度说明：GLB 的 3.81322 × 2.33144 × 1.39608 m 仅为转换资产展示包围盒；由于缺少测量控制点，实建尺度保持未验证。
4. 扩充参考资产合同，加入视觉优先级、性能预算、验收条件、团结乡样本身份和物理尺度警告；其中对动画、骨骼和 morph target 的初始判断已由 5.3.3 直接审计纠正。
5. Three.js 加载结果新增样本编号、产地依据、物理尺度状态和包围盒含义，避免宿主把扫描分片误当 BIM 构件。

## 5.3.1

1. 将用户提供的“民居 C2 美人靠”接入本地 Three.js 3GIS 参考层。
2. 在 `references-private/c2-meirenkao/` 保存源 FBX、RealityCapture 元数据、源贴图和 High/Lite GLB；公开发布仍默认排除该目录。
3. 新增 C2 资产契约、来源哈希、空间分组到生产阶段的观察映射，以及本地导入 QA 报告。
4. 新增 `threejs/C2MeirenkaoAsset.js`，Lite 优先并在 Draco 解码失败时回退 High；保留“空间参考，不宣称 BIM 语义”的边界。

## 5.3.0 工作中纠错

1. 依据用户复核，纠正一颗印东西小厦屋面坡向：高边贴耳房、低边向天井。
2. 缩短小厦与正房大厦的转角重叠，消除俯视中的错误穿插。
3. 将南向外立面、东/西侧立面、院内正房立面拆分为三套正投影逻辑。
4. 正房横剖面恢复以脊檩为中心的对称双坡。
5. 新增基础图纸纠错台账，未获得证据的门窗、节点和雕刻继续锁定。
6. 双扇门单扇宽度改为门洞净宽的一半。
7. 主房、耳房及三开间前廊个案的山墙围护补至脊下。
8. 山墙封闭改为按墙体平面与屋面下皮求交，消除屋面出挑导致的大楔形缝隙。
9. 入口门洞上方补回连续实体墙身，不再显示贯通空洞。
10. 屋面由任意间距线稿改为逐件板瓦—筒瓦系统；接入板瓦长约 223 mm、大口/小口约 242/221 mm，筒瓦长约 222 mm、大口/小口约 115/90 mm，以及椽子 70×80@210。
11. 廊柱与主要木柱改为圆木柱身配方形石柱础，梁枋继续保持矩形截面。
12. 根据最新使用评审撤销陡直 13 级单跑梯，改为 8+8 级双跑日常木梯，补中间平台、上下平台、双侧承托与连续扶手。
13. 天井沿廊恢复开敞，院内房间边界改为木框、板壁与格扇组合的木隔扇。
14. 屋面改为轻微曲面的凹面板瓦、凸面压缝筒瓦和逐件脊瓦实体系统；补连续基层、210 mm 椽列和檐口木板，消除瓦面漏色与人眼下方露瓦背。
15. 外围厚土墙增加从底向上内收的收分；嵌入外墙的木柱与外榀构架在完整外观中遮蔽，关闭墙体时保留施工检查能力。
16. 门扇增加边梃、抹头、裙板和上部格心；双扇单扇仍锁定为洞口半宽，门洞上部保留连续墙身。
17. 新增人眼入户与室内检查模式，支持观察点跳转、鼠标转头、WASD 和滚轮移动，贯通入口、院内/前廊、楼梯和二层。
18. 将 210 mm 明确限定为剖面可读的椽子间距；瓦面精确纵向搭接和节点继续按证据门槛管理，坡向露明量只作视觉校准。
19. 依据用户补充的瓦件草测、昆明历史图录和云南实景屋面，重建带大小口收分、弧面和端头厚度的单片板瓦/筒瓦；删除外部资料与 60 mm 暂定搭接，不再形成连续金属条带观感。
20. 完成本地 WebGL 多视角复查：一颗印和三开间前廊个案均确认单片瓦行、连续基层、无墙色漏底、无柱端穿屋面，并记录 586552/275384 三角面运行规模；精确纵向搭接继续锁定。

## 5.3.0

1. 接入“云南民居三开间带前廊两层建筑”测绘个案。
2. 锁定 11.53 m 面阔、7.92 m 进深、1.87 m 前廊、2.85 m 二层和 7.04 m 屋脊。
3. 新增一层/二层平面、正立面、控制剖面、梁架、屋面与施工门控视图。
4. 新增 WebGL 参数化单体，四副横向梁架按两山穿斗式、中间两副抬梁式分榀。
5. 主体悬山与前廊披檐保持独立，厚土坯围护与木构架分层。
6. 14 张用户参考图进入本地私有证据目录，公开数据仅保留文件名、哈希、证据主张与版权边界。
7. 用户提供的本地瓦件草测、历史图录和屋面实景作为本轮瓦作的唯一主动证据链。
8. 新增运行预算合同和个案验证记录。

## 5.2.1

1. 主显示器使用 WebGL 深度缓冲。
2. 修复旋转视角的错误遮挡和穿面。
3. 院内观察只改变镜头。
4. 剖切改为独立诊断模式。
5. 人工旋转时恢复完整建筑几何。
6. 双扇大门、正房上层门和高窗加入连续开合动画。
7. 人物路线贯通大门、倒座、天井、小厦、楼梯巷、大厦和正房二层。
8. 二十四项浏览器验收全部通过。
9. 新增 GitHub Pages 自动部署、完整本地启动工具和仓库验证工具。

## 5.2.0

1. 首次加入 WebGL 深度缓冲显示器。
2. 首次加入门窗自动演示和人物入户上楼路线。

## 5.1.0

1. 默认显示完整建筑。
2. 增加外观、院内、屋顶和正立面镜头。

## 5.0.0

1. 建立云南院落民居母系统。
2. 接入一颗印测绘平面、立面和控制剖面。
3. 纳西民居作为滇西支系进入生产线。
