# Tiles Mother · Microscope 核心接入准备（非预览版）

本目录不是新工作台，不替换当前 08A 页面，不继承被用户否决的 R2-11。

已从用户 2026-09-06 直接教学的 CORE_01_02.md 找回并实现坐标约定、17 个二倍尺度、余弦核、前缀和及完整链式导数。具体来源见 SOURCE_AND_SCOPE.json。没有加入普通噪声、刻线、装饰孔洞或自行猜测的旋转参数。

PBR 适配器接收既有底色、粗糙度、法线以及 Microscope 的值和梯度；不生成瓦体，不修改轮廓、尺寸、搭接、木构、年份或镜头。通道增益是适配参数，不冒称原作常数或实测值。当前尚未接入瓦面工作台。

## 实际验证

`node --check microscope-core.mjs` 和 `node test-microscope.mjs` 已实际运行，14 项 CPU 检查通过。测试仅依赖 Node.js 标准库。

原生 Mesa GLES 3.2 软件上下文中实际完成 GLSL ES 3.00 编译、链接、绘制和浮点读回。核心取样为 4 个位置 × 4 个层数，共 16 例。独立 PBR 适配固定相同字段输入后 24 例通过，最大绝对误差约 6.08e-8。该原生检查不是浏览器或 iPhone 验收。

17 层不加过滤的 CPU 双精度 / GLES 单精度交叉比较，在一个高增益测试例出现法线分量差约 0.001769，超出初始 0.001 目标；完整交叉精度门仍为 false，不能把隔离后的 PBR 测试通过冒充端到端全通过。见 QA_SUMMARY.json。

## 接入前阻塞

找回的教学只包含核心一、二，文件本身标记核心三、四待接收。用户这次强调的完整旋转节点数值还没有核实，不能补猜，也不能把本目录称为全部原作。

仍需核实真实瓦面的坐标域、角向极点避让、局部/世界梯度换算、屏幕过滤及滑条回归。GLSL 的 footprint 权重属于显示适配，当前只核验零 footprint 下的原式前缀，不证明完整抗混叠。

当前 START_HERE.html 未修改；形状、尺寸、搭接和当前公开工作台保持原样。尚无新的公网预览。所有视觉、生产批准字段保持 false。
