# Tiles Mother · R2 收尾候选 07

从 R2-closeout-06-handmade 接续，只增加现有 Microscope 材料路径的局部刮抹、压实和稀疏麻面。材质面板的“对照 A · 06 / 对照 B · 07”在相机、光线、种子与场景不变时切换最后细节，不重新生成瓦片。

大轮廓、尺寸、主弧度、收分、几何厚度、搭接支承、木构、0/3/5/7/10 年失养逻辑和移动端工作台架构保持原样。本版也没有提高整坡细节强度来制造远景噪点：低于显示分辨率的细节由原有 footprint 规则淡出，近看使用同一材料函数。

参考身份：讲武堂瓦片精细(2).zip，对应本机同哈希原件；SHA-256 `ae5510c0e2eaec236adff0b94d978688f6c17a9412407c6c7ec54968222dd365`。四个内部文件均经过字节数与 SHA-256 核验。Diffuse 7000×7000 与 normal 2500×2500 已实际解码。局部纹理中观察到中断的方向性痕迹、密度不同的麻面与较平缓区域。形成方式属于解释，微起伏幅度与粗糙度是设计参数，不能从烘焙明暗当作实测反推；未公开原件或大贴图。

验证：JavaScript 语法通过；原有几何、顶点/深度着色器及完整场景/失养代码逐段一致。原生 OpenGL 编译和离屏渲染通过，使用当前源码导出的实际场景与着色器，仅转换 ES/桌面 GLSL 声明。单片 A 与原版 06 渲染逐像素相同；B 有可见局部变化。板瓦、筒瓦、小屋面、860 片、木构均无 GL 错误；木构 A/B 相同。0/3/5/7/10 年 860 片场景的 unsupportedLive 均为 0。这些是指定种子和场景的回归结果，不是新增物理精度证明。

浏览器控制连接不可用：两次清单读取均返回 nodeRepl.fetch request failed，创建标签页超时。原生 GPU 检查不能替代浏览器交互、移动端或公网验收，因此 `browserVerified=false`、`publicBrowserVerified=false`、`visualApproved=false`、`productionApproved=false`。尚不能称作已完成公网交付或用户视觉通过。

沿用固定 SHA 的 raw.githack HTTPS 入口，不覆盖旧版。需要补验：实际公网加载、07 版本标识、同机位 A/B、场景切换、旋转缩放、移动端面板及近景细节稳定性。

共同规则原件副本位于 rules/，后续接续应携带。用户本轮已在暂停说明后明确要求继续按交接工作，此授权用于 Tiles Mother 当前细节任务；不表示其他 Mother 的隔离目录已完成清理。
