# 讲武堂精细瓦片：原件重新接入与保留决定

2026-09-05，Tiles Mother。生产工作分支 feature/tiles-mother-v0.1-workbench，开始回读 HEAD be41739780e1ea01781271bc2a6ab53c2dad64f4。本次收到用户重新上传的原始 ZIP，直接读取字节、几何数组及原分辨率贴图。产品基线继续为 V0.9.8，未改生成器、材质、灯光、HTML 或批准状态。

## 身份与保留决定

沿用已有资料身份 jiangwutang-001。讲武堂瓦片精细(1).zip 为 58,671,527 字节，SHA256 为 ae5510c0e2eaec236adff0b94d978688f6c17a9412407c6c7ec54968222dd365。与本目录 file-checksums.json 中旧原件一致；内部四个有效文件的大小、SHA256 也全部一致，ZIP CRC 检查通过。这是同一份资料的重新送达，不能计为新增的独立历史证据。

该包仍有用，当前工作环境保留本次上传的 ZIP 和用于后续对照的解包数据。本次删除文件数为 0，释放字节数为 0。原件、完整贴图和扫描网格均不推入 Git，不成为程序化工作台的运行时依赖。用户电脑的原件、聊天附件及其他生产线文件均未触动。工作环境副本的可访问性不等于永久存储保证；后续精确复查依赖仍可访问的原件。

完整参考的细节提炼与新版工作台对照尚未结束，当前没有将其判为无用。将来确需清理时，按 AGENTS 的条件和明确文件清单执行。

## 本轮实际读到的内容

01.fbx：349,088 字节，二进制 FBX 7700。直接解析得到一个 Geometry、一个 Mesh Model、4,901 个控制点、9,508 个三角形和 28,524 个面角。UV 按 ByPolygonVertex / IndexToDirect 读取。所有读取的坐标、UV 和法线数组为有限值。

这里运行的是本地有限数组解析器，未运行 Blender、Three.js FBXLoader 或完整 FBX 场景求值。4,901 是原文件控制点数，旧报告的 28,524 是导入后的非索引顶点数，两种口径不能当成版本差异。本轮也未完成单片瓦的语义分割或扫描边界的成型证据验证。

新增读取到的原文件元数据：GlobalSettings 中 UnitScaleFactor=100、OriginalUnitScaleFactor=1；Model 的 PreRotation=(-90,0,0)。这些是导出声明，尚未与实测尺寸和实际导入坐标闭环。继续保留物理尺度未验证，不将原文件的坐标数值直接写成米制构造尺寸。

01_u1_v1_diffuse.png：7000×7000 RGBA，完整原生像素已解码。01_u1_v1_normal.png：2500×2500 RGBA，完整原生像素已解码。01.fbx.rsInfo 的原文也已读取。

按 FBX 三角形 UV 重新计算有效像素覆盖，排除没有三角形覆盖的纹理背景。使用 U 到 X、V 到倒置 Y 的整数像素中心采样；结果与旧报告的关键数值一致：

颜色图有效区 30,432,933 像素，占 62.1080265%；有效区平均 RGB=(88.7763,78.9414,71.9805)，标准差=(45.1148,44.3443,48.0845)。法线图有效区 3,879,676 像素，其中 3,853,421 个像素精确等于 [128,128,255]，占 99.3232682%。两张图有效像素最大覆盖计数均为 1。

这些数值是纹理像素统计。它们不等于整片瓦群体配色比例、物理表面积比例、实测粗糙度或真实孔深，也不能据法线图接近基准色断言实物光滑。

## 直接看过的局部及其用途

除整体纹理图外，本轮逐一查看三块不缩放的 800×800 原像素裁切。原图裁切框以左上角为原点，格式为 x0,y0,x1,y1：

A=(1000,500,1800,1300)，B=(3450,1600,4250,2400)，C=(5000,3650,5800,4450)。其 UV 有效区域分别约占 73.35%、77.14%、79.15%。裁切包含纹理岛外的模糊填充区，填充区明确排除在材质与轮廓证据之外。

可观察内容：暖赭与青蓝灰/深灰形成较大色区，区域之间有浅灰覆盖和不规则过渡；部分局部有细长条痕、细裂纹状暗线、颗粒状斑点、剥蚀状边界和沿边的明暗/颜色差异。这些层级适合指导程序化色层、局部事件、表面细节与边缘质感。

推断限制：条痕的制造方向、剥蚀成因、深色是否污染或阴影，以及绿色情况是否生物附着，本轮均未得到独立确认。纹理岛边界不等于实物自由边；不能照着 UV 岛的破碎轮廓制造崩口。真实轮廓应结合对应几何、线描和视角确认。

对本线的直接要求：大色区、片内中尺度斑驳、微细条痕/颗粒和局部边口事件分别控制；保留区域间变化，不能只铺平均色，也不能把满屏高频噪声当作全部细节。先用锁定材质检查新主形，再单独试材质候选。以上是参考提炼，未声称已接入或验证新版工作台。

## 两张线描的补充定位与纠错

前一轮用户上传的两张线描仍保留。印刷图用于辨认构件和图示构造；手绘图用于核对标注尺寸与端部关系；本精细包主要补充外观层级与扫描几何证据。三个来源分开登记，不默认同一测绘对象，不混合平均尺寸。

撤回上一条答复中“筒瓦边厚约3cm”的推断，也撤回将板瓦的5cm高度移给筒瓦和将手绘板瓦长度概述为21cm的说法。手绘板瓦可辨标注为长度23.8cm、两处宽度24.2cm与22.1cm、高度H1=5cm、厚度1.2cm；筒瓦的22.2cm长度和H2=3.2cm属于各自标注位置，3.2cm不能当作壁厚。H1手写读数、两处宽度的内外口或端位语义及套口关系仍需谨慎确认。这次不据读图直接改生产参数。

线描也不足以证明所有瓦都有卷边、顶面压平或固定的自然不对称；上述旧答复中的造型描述降为候选，须与相应实物或明确剖面核对。

## 状态和恢复

sourceReadVerified=true；nativeTextureReadVerified=true；rawGeometryArraysRead=true；historicalNumericComparisonRecorded=true；fullSceneImporterRun=false；newWorkbenchComparisonRecorded=false；distillationComplete=false；temporaryCopiesRemoved=false；visualApproved=false；productionApproved=false。

本次有价值的成果是原件重新可访问、旧身份与关键统计被独立重算确认、局部视觉依据被明确定位、旧读图错误被纠正。新造型、材质升级及浏览器验收尚未由本次接入完成。

本轮可复现脚本和机器记录保存在随本次答复提供的轻量核验包，包内不含原始 ZIP、扫描网格或完整贴图。仓库原有 parse_fbx.mjs 和 analyze_effective_regions.py 继续保留，历史分析不被本次记录覆盖。
